<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newburgers="{{devoured}}">
		{{#if devoured}}EATEN!{{else}}DEVOUR IT!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>